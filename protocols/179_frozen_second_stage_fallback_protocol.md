# Frozen second-stage fallback protocol

The full-node V7 run is technically complete but only five of six new labels
pass the unchanged steady-response platform. Consequently, Protocol 160 is
closed without a refit.

Before running any additional CFD, this document freezes one sequential
continuation:

- geometry: `alph15-10929-3036`;
- new fidelities: `F010`, `F100`, and `F110`;
- endpoint: the unchanged iterations-9001--10000 steady-response platform;
- no validation or sealed-test response may be read.

The geometry is not selected from the observed response magnitude. It is the
third entry in the fallback order frozen by Protocol 138 before the fallback
CFD results existed. Together with the three currently accepted complete
lattices, it is the next candidate capable of restoring affine geometry rank
3.

If all three labels pass, one new Protocol-179 model comparison may be run
with the architecture, seeds, loss, and gates copied unchanged from Protocol
160. If any one fails, this confirmation branch terminates without refitting.
A successful result is described as sequential prospective confirmation, not
as an independent success of Protocol 160.
